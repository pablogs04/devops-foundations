services:
  web:
    image: nginx:alpine
    container_name: web
    ports:
      - "8082:80"
    volumes:
      - ./nginx-static:/usr/share/nginx/html:ro
      - ./nginx-static:/usr/share/nginx/html:ro

